//your JS code here. If required.
window.onload = function() {
			var browserInfo = "You are using " + navigator.appName + " version " + navigator.appVersion;
			document.getElementById("browser-info").innerHTML = browserInfo;
		}